<%= javascript_include_tag :defaults %>

<div class="CtrCol" style="z-index:10">

<%= form_tag(query_path(1),:method => :get)%>

<%= text_field_with_auto_complete :city, :name, 
	{:tabindex => 0, :size => 65}, {:tokens => [';','+'], :min_chars => '3', :method => :get, :order => 'population DESC'} %>
	<input  type="image" src="/images/dj_button.png" alt="GO!" style="position:relative; left:5px; top:20px">
<br
<span style="position: relative; left:-5%; color: silver; z-index:-5"> Type in the City or Country for each participant in your meeting. </span>

<div class="externals" style="z-index:-5">
